<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All resources
    </h1>

    <a href="/resources/new" class="btn btn-block btn-outline-secondary">
      Add a new resource
    </a>
  </div>
</div>

<!-- Ransack Code start -->
<div class="row">
  <div class="col-md-12 mb-3">
    <%= render partial: "resource_templates/resources_search_form" %>
  </div>
</div>
<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Category
        </th>

        <th>
          Tab
        </th>

        <th>
          Resource
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @resources.each do |resource| %>
      <tr>
        <!-- Display resource.id start -->
        <td>
          <%= resource.id %>
        </td>
        <!-- Display resource.id end -->

        <!-- Display resource.category start -->
        <td>
          <%= resource.category %>
        </td>
        <!-- Display resource.category end -->

        <!-- Display resource.tab_id start -->
        <td>
          <% if resource.tab.present? %>
            <a href="/tabs/<%= resource.tab.id %>">
              <%= resource.tab.title %>
            </a>
          <% end %>
        </td>
        <!-- Display resource.tab_id end -->

        <!-- Display resource.resource start -->
        <td>
          <%= resource.resource %>
        </td>
        <!-- Display resource.resource end -->

        <!-- Display resource.created_at start -->
        <td>
          <%= time_ago_in_words(resource.created_at) %> ago
        </td>
        <!-- Display resource.created_at end -->

        <!-- Display resource.updated_at start -->
        <td>
          <%= time_ago_in_words(resource.updated_at) %> ago
        </td>
        <!-- Display resource.updated_at end -->

        <td>
          <a href="/resources/<%= resource.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @resources, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
