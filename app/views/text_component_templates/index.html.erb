<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All text components
    </h1>

    <a href="/text_components/new" class="btn btn-block btn-outline-secondary">
      Add a new text component
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Tab
        </th>

        <th>
          Header
        </th>

        <th>
          Content
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @text_components.each do |text_component| %>
      <tr>
        <!-- Display text_component.id start -->
        <td>
          <%= text_component.id %>
        </td>
        <!-- Display text_component.id end -->

        <!-- Display text_component.tab_id start -->
        <td>
          <% if text_component.tab.present? %>
            <a href="/tabs/<%= text_component.tab.id %>">
              <%= text_component.tab.title %>
            </a>
          <% end %>
        </td>
        <!-- Display text_component.tab_id end -->

        <!-- Display text_component.header start -->
        <td>
          <%= text_component.header %>
        </td>
        <!-- Display text_component.header end -->

        <!-- Display text_component.content start -->
        <td>
          <%= text_component.content %>
        </td>
        <!-- Display text_component.content end -->

        <!-- Display text_component.created_at start -->
        <td>
          <%= time_ago_in_words(text_component.created_at) %> ago
        </td>
        <!-- Display text_component.created_at end -->

        <!-- Display text_component.updated_at start -->
        <td>
          <%= time_ago_in_words(text_component.updated_at) %> ago
        </td>
        <!-- Display text_component.updated_at end -->

        <td>
          <a href="/text_components/<%= text_component.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @text_components, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
